enum ColumnSortState { none, ascending, descending }

enum CustomRowCellIconPlacement { left, right }

enum CustomListTotalCountPosition { top, bottom, none }

enum ColumnDefinitionType { text, number, date, custom }
